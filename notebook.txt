## Notes
 
- Persistence
- 2-Fault Tolerance
- Multi-machine readiness (simulated if needed)
- No shared storage
- Bonus: Log inspection & system recovery

For 2-fault tolerance, we should have 3 servers on different ports.
Each follower is an independent replica with its own persistent storage and message logs. There is no shared store.

The leader coordinates replication. Leader election is done based on highest id. Each server keeps own copies of everything

There was a problem during which the servers thought that the messages coming from the leader are from the follower
This kept it bouncing back and forth

Had the servers force an election when they joined, this was a problem when the new node with a highest id joined
Now the new servers wait for a heartbeat

